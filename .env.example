# Copia este archivo como `.env` y pon tus valores reales.
# El archivo .env está en .gitignore: nunca llega a GitHub.

# Token de la API de Apify (obligatorio para fetch.py — Fase 1)
APIFY_TOKEN=pon_aqui_tu_token

# Key de la API de Anthropic (OPCIONAL — solo para la capa narrativa con IA, Fase 6)
# Si no está, el reporte se genera igual sin la narrativa.
# ANTHROPIC_API_KEY=
